h3 Update your books

a(href='/books') Back

form(action='/books/update/result', method="get")
  input(type='text', name="title", placeholder="Your new title", value=`${updateBook.title}`)
  input(type='hidden', name="id", value=`${updateBook.id}`)
  button Update